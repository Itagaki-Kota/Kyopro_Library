import sys
sys.set_int_max_str_digits(0)  # 桁数無制限


【文字列と整数の変換】が4300ケタまでになった。(ローカルのバージョンでは動作しても, コンテストサイトのバージョンで動作するか？)
int(input())や, 他にも内部でこの変換を行っている組み込み関数なども。

勘違いしないでほしいのは
数自体は, Pythonは多倍長整数でどこまでも大きな数を扱える。(計算は遅くなるし, メモリの許す限りだけれども。)
ここでの「桁数制限」とは
「整数と文字列の変換処理」に設定される桁数制限である。